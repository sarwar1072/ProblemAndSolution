@using ProblemAndSolution.Web.Utilities

@if(TempData.Peek<ResponseModel>("ResponseMessage") != null)
{
    var response = TempData.Get<ResponseModel>("ResponseMessage");
    <div class="alert alert-@(response.Response.ToString().ToLower())" role="alert">
        @response.Message
    </div>
}